import { InvalidCredentialsFilter } from './invalid-credentials.filter';

describe('InvalidCredentialsFilter', () => {
  it('should be defined', () => {
    expect(new InvalidCredentialsFilter()).toBeDefined();
  });
});
